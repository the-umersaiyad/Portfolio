import postgres from "postgres"; import * as dotenv from "dotenv"; dotenv.config({ path: ".env.local" }); const sql = postgres(process.env.DATABASE_URL!); async function run() { await sql`CREATE TABLE IF NOT EXISTS "social_links" ("id" serial PRIMARY KEY NOT NULL, "platform" text NOT NULL, "url" text NOT NULL, "order" integer NOT NULL);`; console.log("Created social_links table"); process.exit(0); } run();
